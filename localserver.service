[Unit]
Description=local server
After=network.target

[Service]
User=webadmin  # Replace with the username you want to run the service as
ExecStart=/home/webadmin/start_localserver.sh

[Install]
WantedBy=multi-user.target
